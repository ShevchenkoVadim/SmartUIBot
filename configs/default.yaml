capture:
  backend: auto        # auto | mss | dxcam
  target_fps: 60
  monitor: 1
detection:
  model: yolo11n.pt
  confidence: 0.35
  device: auto         # auto | cpu | cuda
  tracking: false
  smoothing_frames: 3
ui:
  preview_max_width: 960
logging:
  level: INFO
  dir: logs
hotkeys:
  emergency_stop: "<ctrl>+<alt>+q"
